"""
module with features necessary for work
"""
# pylint: disable=invalid-name
import pandas as pd


def add_synthetic_feature1(df: pd.DataFrame) -> pd.DataFrame:
    """
    The function adds a feature to df
    one function - one feature
    is necessary for a convenient combination of them when researching
    :param pd.DataFrame df: df to add a feature
    :return: updated df
    """
    return df


def add_synthetic_feature2(df: pd.DataFrame) -> pd.DataFrame:
    """
    similar to earlier
    :param df:
    :return:
    """
    return df


def add_synthetic_feature3(df: pd.DataFrame) -> pd.DataFrame:
    """
    similar to earlier
    :param df:
    :return:
    """
    return df
